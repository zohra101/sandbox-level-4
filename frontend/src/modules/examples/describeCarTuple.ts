export function describeCarTuple(carInfo: [string, number, boolean]) {
    const [color, year, isForSale] = carInfo;
    return;
    // console.log(`Your car is ${color}, made in ${year}, and for sale is ${isForSale}.`)
};
